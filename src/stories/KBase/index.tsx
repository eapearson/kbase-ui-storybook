import React from 'react';
import { storiesOf } from '@storybook/react';

export default function() {
    storiesOf('KBase', module).add('Overview', () => {
        return (
            <div className="storyWrapper">
                <h3>KBase</h3>
                <p>TO BE DONE</p>
            </div>
        );
    });
}
