import React from 'react';
import { storiesOf } from '@storybook/react';

export default function() {
    storiesOf('Ant Design/DataDisplay/Tabs', module).add('Overview', () => {
        return (
            <div className="storyWrapper">
                <h3>Tabs</h3>
                <p>TO BE DONE</p>
            </div>
        );
    });
}
