import React from 'react';
import { storiesOf } from '@storybook/react';

export default function() {
    storiesOf('Ant Design/DataDisplay/Collapse', module).add('Overview', () => {
        return (
            <div className="storyWrapper">
                <h3>Collapse</h3>
                <p>TO BE DONE</p>
            </div>
        );
    });
}
