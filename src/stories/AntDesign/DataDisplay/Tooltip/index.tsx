import React from 'react';
import { storiesOf } from '@storybook/react';

export default function() {
    storiesOf('Ant Design/DataDisplay/Tooltip', module).add('Overview', () => {
        return (
            <div className="storyWrapper">
                <h3>Tooltip</h3>
                <p>TO BE DONE</p>
            </div>
        );
    });
}
